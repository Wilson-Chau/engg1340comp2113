#include <iostream>

using namespace std;

double average( int x, double y ){
    return (x + y) / 2;
}

int main()
{
    int num1;
    double num2;
    cin >> num1 >> num2;
    
    cout << average(num1, num2) << endl;

    return 0;
}
