#include <iostream>

using namespace std;

int main()
{
    int a = 4, b = 3, c = 2;
    a += b += c    // a = 9, b = 5, c = 2
    a *= b *= c    // a = 24, b = 6, c = 2
    a += b %= c    // a = 5, b = 1, c = 2
    (a += b) *= c  // a = 14, b = 3, c = 2
    
    return 0;
}
