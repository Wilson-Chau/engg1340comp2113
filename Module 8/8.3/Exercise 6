Node *get_item(Node *head, int num){
    Node *current = head;
    int idx = 0
    
    while(current != NULL){
        if(idx == k)
            return current;
        
        current = current->next;
        idx++;
    }
    
    return NULL;
}
