#include <iostream>
#include <string>

using namespace std;

int main()
{
	string str;
	getline(cin, str);
	
	int length = 0;
	while (!str.empty()){
	    length++;
	    str.erase(0, 1);
	}
	
	cout << "length of input string: " << length << endl;

	return 0;
}
